# Daily Audit Report — 2026-07-20

| Check | Status |
|-------|--------|
| Build | PASS (647 pages, 2.41s) |
| Links | PASS |
| Mobile | PASS |
| Dates | PASS (no future dates) |
| SEO | PASS (0 warnings) |
| Images | PASS (0 warnings) |
| Sitemap | PASS (162 URLs, all canonical) |
| RSS | PASS (canonical URLs, en-US, enclosures on 53 of 54 items) |
| JSON-LD | PASS (NewsArticle + BreadcrumbList) |
| OG/Twitter | PASS (full metadata on all pages) |
| Canonical | PASS (all news.lesbass.com, no legacy host refs) |
| Live site | PASS (200 OK, Cloudflare, HTTPS) |
| Browser screenshots | **BLOCKED** (missing libglib-2.0.so.0) |

**Result:** All automated checks pass. No errors. No warnings. Browser visual verification blocked by infra.

## Fixes Applied This Run

### 1. Image credit for SWE-Bench Pro article ✅

Added `imageAlt` and `imageCredit` frontmatter to `openai-swe-bench-pro-audit-30-percent-broken.md`. The article previously had an `image` field but no alt text or credit caption. Resolved the lone image warning.

### 2. Repo hygiene: removed `homepage/` scratch dir from root ✅

Removed `homepage/` directory (two screenshots) from the repository root. Screenshot artifacts belong in the tooling directory or issue thread, not the repo root.

## Previous Run Fixes (commit `84167cf`)

- 5 Italian-word article slugs renamed to English
- 5 Cloudflare 301 redirect rules added to `public/_redirects`

## Outstanding Issues

### 1. Duplicate Leanstral articles (High — duplicate content)

Two articles cover the same event (Mistral Leanstral 1.5 release, 2026-07-04):

- `leanstral-mistral-open-source-proof-engineering.md`
- `mistral-leanstral-1-5-proof-engineering-model.md`

Both have near-identical descriptions and sources. One should be merged or one designated canonical with the other 301'd.

### 2. Browser screenshot infrastructure

Playwright Chromium fails due to missing system library `libglib-2.0.so.0`. Visual verification requires a real browser. This is a persistent infra issue — the environment does not support real browser rendering.

## Verified Strengths

- All canonical URLs use `https://news.lesbass.com/`
- Sitemap: 162 URLs, all canonical, image extension, correct lastmod/priority/changefreq
- RSS: all item links and GUIDs use canonical domain, en-US language, enclosure images on 53 of 54 items
- robots.txt: clean allow-all, correct sitemap reference
- Full OG/Twitter metadata on homepage and article pages
- NewsArticle JSON-LD on article pages with full metadata
- BreadcrumbList JSON-LD on article pages
- Mobile: viewport meta, responsive images, scrollable code blocks, touch targets, skip-to-content
- No broken internal links
- No future publication dates
- All 54 articles have at least one image with alt text and credit
- Security headers: HSTS (1yr + preload), X-Frame-Options DENY, X-Content-Type-Options nosniff, Permissions-Policy
- Cloudflare CDN with cache
- All 5 SEO/image/dates/link/mobile check scripts pass with zero errors and zero warnings

## Build

```
647 page(s) built in 2.41s
```

## Live Site

```
HTTP/2 200 — cloudflare, cf-cache-status: HIT, strict-transport-security, x-frame-options: DENY
```

## RSS

Latest item: Moonshot AI releases Kimi K3 (2026-07-19). No article published on 2026-07-20.
