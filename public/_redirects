# Force www to apex — configure via Cloudflare Dashboard > Rules > Redirect Rules:
# Source: www.news.lesbass.com/*  →  Destination: https://news.lesbass.com/:splat  (301)
# The _redirects file only supports path patterns, not domain redirects.
